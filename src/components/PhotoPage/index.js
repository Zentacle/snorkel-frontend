import styles from "components/PhotoPage/PhotoPage.module.css"
import Layout from "components/Layout/Layout";
const PhotoPage = () =>{
    return (
        <Layout>
        <div>this is the photopage</div>
        </Layout>
    )
}

export default PhotoPage;